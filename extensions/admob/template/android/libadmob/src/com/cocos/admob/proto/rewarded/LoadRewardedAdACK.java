package com.cocos.admob.proto.rewarded;

import com.cocos.admob.proto.Base;
import com.cocos.admob.proto.ILoadAdError;

public class LoadRewardedAdACK extends Base {

    public LoadRewardedAdACK(String unitId) {
        super(unitId);
    }
}
