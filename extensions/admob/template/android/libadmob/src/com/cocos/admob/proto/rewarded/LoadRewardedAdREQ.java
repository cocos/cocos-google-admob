package com.cocos.admob.proto.rewarded;

import com.cocos.admob.proto.Base;

public class LoadRewardedAdREQ extends Base {

    public LoadRewardedAdREQ(String unitId) {
        super(unitId);
    }
}
