import { FullScreenContentCallback } from "./FullScreenContentCallback";

/**
 * @en
 * FullScreenContentCallback 的别名，供开屏广告使用
 * @en
 * Alias for FullScreenContentCallback, only work with App Open Ad.
 * 
 */
export type AppOpenAdFullScreenContentCallback = FullScreenContentCallback;